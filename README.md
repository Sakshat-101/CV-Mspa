# 😎 Facial Expression Detection using Python & OpenCV

This project detects human facial expressions from images using a pre-trained **FER (Facial Emotion Recognition)** model with **OpenCV**.  
It classifies emotions like *happy*, *sad*, *angry*, *surprise*, *fear*, *disgust*, and *neutral*.

---

## 🚀 Features
- Detects faces in images.
- Classifies facial expressions using deep learning (FER model).
- Draws green boxes and emotion labels.
- Supports multiple emotions and outputs processed images.

---

## 🧰 Requirements
Install dependencies:

```bash
pip install -r requirements.txt
